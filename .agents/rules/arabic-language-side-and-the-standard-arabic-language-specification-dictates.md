---
trigger: always_on
---

It should be noted that the application has an Arabic language side, and the standard Arabic language specification dictates that text, buttons, tables, titles, and outputs are all from right to left. Therefore, you should ensure that this is correct after completing any task.
# The direction of texts in Arabic is from right to left.
# Text placement within any page in Arabic is from right to left.
# The correct location for buttons in Arabic is on the right side of the page.
# Replace the hard codes with the correct Arabic language keys.
# the pdf & excel report exported in arabic must be from right to left direction
# any new title or sub title or content add it on during development must be mirroring at the arabic side from right to left.